// Generated by CIRCT firtool-1.128.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module ArithmeticCallerModule(
  input         clock,
  input         reset,
  input         io_kick,
  input  [31:0] io_lhs,
  input  [31:0] io_rhs,
  input         io_calleeBusy,
  input         io_calleeDone,
  output        io_calleeStart,
  output [31:0] io_calleeLhs,
  output [31:0] io_calleeRhs,
  output        io_finished
);

  reg  [2:0]  state;
  reg  [31:0] lhsReg;
  reg  [31:0] rhsReg;
  wire        io_finished_0 = state == 3'h4;
  wire        _GEN = state == 3'h0;
  wire        _GEN_0 = state == 3'h1;
  wire        _GEN_1 = io_kick & ~io_calleeBusy;
  always @(posedge clock) begin
    if (reset) begin
      state <= 3'h0;
      lhsReg <= 32'h0;
      rhsReg <= 32'h0;
    end
    else begin
      if (_GEN) begin
        if (_GEN_1)
          state <= 3'h1;
      end
      else if (_GEN_0) begin
        if (io_calleeBusy) begin
        end
        else
          state <= 3'h2;
      end
      else if (state == 3'h2) begin
        if (io_calleeDone)
          state <= 3'h3;
      end
      else if (state == 3'h3)
        state <= 3'h4;
      else if (io_finished_0 & ~io_kick)
        state <= 3'h0;
      if (_GEN & _GEN_1) begin
        lhsReg <= io_lhs;
        rhsReg <= io_rhs;
      end
    end
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:2];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [1:0] i = 2'h0; i < 2'h3; i += 2'h1) begin
          _RANDOM[i] = `RANDOM;
        end
        state = _RANDOM[2'h0][2:0];
        lhsReg = {_RANDOM[2'h0][31:3], _RANDOM[2'h1][2:0]};
        rhsReg = {_RANDOM[2'h1][31:3], _RANDOM[2'h2][2:0]};
      `endif // RANDOMIZE_REG_INIT
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_calleeStart = ~_GEN & _GEN_0 & ~io_calleeBusy;
  assign io_calleeLhs = lhsReg;
  assign io_calleeRhs = rhsReg;
  assign io_finished = io_finished_0;
endmodule

