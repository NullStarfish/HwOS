// Generated by CIRCT firtool-1.128.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module ForkNonBlockingModule(
  input         clock,
  input         reset,
  input         io_start,
  output [31:0] io_parentPC,
  output [31:0] io_mainReg,
  output [31:0] io_childReg,
  output        io_done
);

  wire        startWire = io_start;
  wire        abortWire = 1'h0;
  wire        abortWire_1 = 1'h0;
  reg         activeReg;
  wire        activeReg_0 = activeReg;
  wire        abortWire_0 = abortWire;
  reg  [31:0] accReg;
  wire [31:0] accReg_0 = accReg;
  reg  [31:0] mainReg;
  wire [31:0] mainReg_0 = mainReg;
  reg         activeReg_1;
  reg  [2:0]  pcReg;
  wire        _GEN = pcReg == 3'h3;
  wire        doneWire_1 = ~abortWire_1 & activeReg_1 & _GEN;
  reg  [2:0]  pcReg_1;
  wire        startWire_1 = ~abortWire & activeReg & pcReg_1 == 3'h0;
  wire        _layer_probe = pcReg_1 == 3'h1;
  wire [32:0] _GEN_0 = {1'h0, mainReg};
  wire [32:0] _mainReg_T = _GEN_0 + 33'h1;
  wire        _layer_probe_0 = pcReg_1 == 3'h2;
  wire [32:0] _mainReg_T_2 = _GEN_0 + 33'h1;
  wire        _layer_probe_1 = pcReg_1 == 3'h3;
  wire [32:0] _mainReg_T_4 = _GEN_0 + 33'h1;
  wire        _layer_probe_2 = pcReg_1 == 3'h4;
  wire        _layer_probe_3 = pcReg_1 == 3'h5;
  wire        doneWire = ~abortWire & activeReg & _layer_probe_3;
  always @(posedge clock) begin
    if (reset) begin
      activeReg <= 1'h0;
      accReg <= 32'h0;
      mainReg <= 32'h0;
      activeReg_1 <= 1'h0;
      pcReg <= 3'h0;
      pcReg_1 <= 3'h0;
    end
    else begin
      activeReg <= ~abortWire & (activeReg ? ~_layer_probe_3 : startWire);
      if (abortWire_1 | ~activeReg_1) begin
      end
      else if (pcReg == 3'h2)
        accReg <= accReg + 32'h5;
      else if (pcReg == 3'h1)
        accReg <= {accReg[30:0], 1'h0};
      else if (pcReg == 3'h0)
        accReg <= 32'hA;
      if (abortWire | ~activeReg) begin
      end
      else if (doneWire_1)
        mainReg <= accReg;
      else if (_layer_probe_1)
        mainReg <= _mainReg_T_4[31:0];
      else if (_layer_probe_0)
        mainReg <= _mainReg_T_2[31:0];
      else if (_layer_probe)
        mainReg <= _mainReg_T[31:0];
      activeReg_1 <= ~abortWire_1 & (activeReg_1 ? ~_GEN : startWire_1);
      if (abortWire_1)
        pcReg <= 3'h0;
      else if (activeReg_1)
        pcReg <= startWire_1 & doneWire_1 | _GEN ? 3'h0 : pcReg + 3'h1;
      else if (startWire_1)
        pcReg <= 3'h0;
      if (abortWire)
        pcReg_1 <= 3'h0;
      else if (activeReg) begin
        if (doneWire_1)
          pcReg_1 <= 3'h5;
        else if (startWire & doneWire | _layer_probe_3)
          pcReg_1 <= 3'h0;
        else if (~_layer_probe_2)
          pcReg_1 <= pcReg_1 + 3'h1;
      end
      else if (startWire)
        pcReg_1 <= 3'h0;
    end
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:2];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [1:0] i = 2'h0; i < 2'h3; i += 2'h1) begin
          _RANDOM[i] = `RANDOM;
        end
        activeReg = _RANDOM[2'h0][0];
        accReg = {_RANDOM[2'h0][31:1], _RANDOM[2'h1][0]};
        mainReg = {_RANDOM[2'h1][31:1], _RANDOM[2'h2][0]};
        activeReg_1 = _RANDOM[2'h2][1];
        pcReg = _RANDOM[2'h2][4:2];
        pcReg_1 = _RANDOM[2'h2][7:5];
      `endif // RANDOMIZE_REG_INIT
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_parentPC = {29'h0, pcReg_1};
  assign io_mainReg = mainReg;
  assign io_childReg = accReg;
  assign io_done = doneWire;
endmodule

